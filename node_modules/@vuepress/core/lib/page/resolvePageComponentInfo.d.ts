import type { MarkdownHeader, MarkdownLink } from '@vuepress/markdown';
import type { App, PageFrontmatter } from '../types';
/**
 * Resolve page component and related info
 */
export declare const resolvePageComponentInfo: ({ app, content, frontmatter, filePathRelative, htmlFilePathRelative, key, }: {
    app: App;
    content: string;
    frontmatter: PageFrontmatter;
    filePathRelative: string | null;
    htmlFilePathRelative: string | null;
    key: string;
}) => Promise<{
    headers: MarkdownHeader[];
    links: MarkdownLink[];
    componentFilePath: string;
    componentFilePathRelative: string;
    componentFileContent: string;
    componentFileChunkName: string;
}>;
